<h1>Inventory</h1>

<table>
  <tr>
    <th>Count</th>
    <th>Product</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_deliveries.each do |product_delivery| %>
  <tr>
    <td><%= product_delivery.count %></td>
    <td><%= product_delivery.product.title %></td>
    <td><%= link_to 'Show', product_delivery %></td>
    <td><%= link_to 'Edit', edit_product_delivery_path(product_delivery) %></td>
    <td><%= link_to 'Destroy', product_delivery, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product delivery', new_product_delivery_path %>
